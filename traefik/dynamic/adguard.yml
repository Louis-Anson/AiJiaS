# AdGuard Home — DNS 管理面板
http:
  routers:
    dns:
      rule: "Host(`dns.aijias.xyz`)"
      entrypoints: websecure
      tls: true
      service: adguard

  services:
    adguard:
      loadBalancer:
        servers:
          - url: "http://192.168.3.195:3000"
